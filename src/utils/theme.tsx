import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: `#002441`,
    },
    secondary: {
      main: `#e95539`,
    },
  },
});

export const mainBackgroundColor: string = `rgb(231, 235, 240)`;
